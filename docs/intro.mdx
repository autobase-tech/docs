---
slug: /
sidebar_position: 1
---

# Introduction

**[Autobase for PostgreSQL®](https://github.com/autobase-tech/autobase)** automates the deployment and management of highly available PostgreSQL clusters in production environments. This solution is tailored for use on dedicated physical servers, virtual machines, and within both on-premises and cloud-based infrastructures.

### Supported setups of Postgres Cluster

import ThemedImage from '@theme/ThemedImage';

<ThemedImage
  alt="postgresql-cluster-scheme"
  sources={{
    light: '/img/postgresql_cluster.png',
    dark: '/img/postgresql_cluster.dark_mode.png',
  }}
/>

For a detailed description of the cluster components, visit the [Architecture](./overview/architecture.md) page.

## Getting Started

#### Deploy the Autobase Console

To run the autobase console, execute the following commands:

:::info
It is recommended to run the console in the same network as your database servers to enable monitoring of the cluster status.
:::

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
  <TabItem value="http" label="Quick start" default>

1. Get docker-compose file:
```
curl -fsSL \
  https://raw.githubusercontent.com/autobase-tech/autobase/refs/heads/main/docker-compose.platform.yml \
  --output ./docker-compose.platform.yml
```

2. Create environment `.env` file:
```yaml
AUTH_TOKEN=secret-token  # Your authorization token
```

3. Run Autobase Console:

```
docker compose -f docker-compose.platform.yml up -d
```

#### Open the Console UI

Go to http://server-address (address of your autobase server) and use `secret-token` for authorization.

  </TabItem>
  <TabItem value="https" label="With SSL (recomended)">

1. Create a DNS A record pointing your domain to the Autobase server IP address.

2. Get docker-compose file:
```
curl -fsSL \
  https://raw.githubusercontent.com/autobase-tech/autobase/refs/heads/main/docker-compose.platform.ssl.yml \
  --output ./docker-compose.platform.ssl.yml
```

3. Create environment `.env` file:
```yaml
DOMAIN=autobase.example.com  # Your domain
EMAIL=admin@example.com      # Email used for ACME/Let's Encrypt
AUTH_TOKEN=secret-token      # Your authorization token
```

4. Run Autobase Console:

```
docker compose -f docker-compose.platform.ssl.yml up -d
```

#### Open the Console UI

Go to https://autobase.example.com (DNS name of your autobase server) and use `secret-token` for authorization.

  </TabItem>
</Tabs>

#### Create your PostgreSQL cluster

Refer to the [Deployment](./docs/category/deployment) section to learn more about the different deployment methods.

:::tip
📩 Contact us at info@autobase.tech, and our team will help you implement Autobase into your infrastructure.
:::
